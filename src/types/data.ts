export interface Data {
  minsk: {
    name: string;
    center: [number, number];
  };
  gomel: {
    name: string;
    center: [number, number];
  };
  brest: {
    name: string;
    center: [number, number];
  };
  vitebsk: {
    name: string;
    center: [number, number];
  };
  grodno: {
    name: string;
    center: [number, number];
  };
  mogilev: {
    name: string;
    center: [number, number];
  };
}
